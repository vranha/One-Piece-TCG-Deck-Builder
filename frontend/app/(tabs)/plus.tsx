// app/(tabs)/plus.tsx
import React from "react";
import { View } from "react-native";

export default function PlusScreen() {
  // Pantalla dummy, ya que se va a sobreescribir el comportamiento del tab.
  return <View />;
}
